# iOS Wrapper Files

These files are starter code for an Xcode SwiftUI wrapper around the Streamlit app.

They do not replace the Python app. They simply open the running Streamlit server inside a `WKWebView` so you can test the mobile feel in the iOS Simulator.

Use `XCODE_IOS_TESTING.md` for the step-by-step workflow.
