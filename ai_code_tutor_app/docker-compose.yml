services:
  ai-code-tutor:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - ALLOW_CODE_RUNNER=false
      - APP_DATA_DIR=data
    volumes:
      - ./data:/app/data
